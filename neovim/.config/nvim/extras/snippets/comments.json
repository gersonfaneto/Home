{
  "NOTE": {
    "prefix": "note",
    "description": "Generate a `NOTE` comment.",
    "body": [
      "$LINE_COMMENT NOTE: $1 - $CURRENT_MONTH.$CURRENT_DATE.$CURRENT_YEAR: $0"
    ]
  },
  "TODO": {
    "prefix": "todo",
    "description": "Generate a `TODO` comment.",
    "body": [
      "$LINE_COMMENT TODO: $1 - $CURRENT_MONTH.$CURRENT_DATE.$CURRENT_YEAR: $0"
    ]
  },
  "WARN": {
    "prefix": "warn",
    "description": "Generate a `WARN` comment.",
    "body": [
      "$LINE_COMMENT WARN: $1 - $CURRENT_MONTH.$CURRENT_DATE.$CURRENT_YEAR: $0"
    ]
  },
  "ERROR": {
    "prefix": "error",
    "description": "Generate an `ERROR` comment.",
    "body": [
      "$LINE_COMMENT ERROR: $1 - $CURRENT_MONTH.$CURRENT_DATE.$CURRENT_YEAR: $0"
    ]
  },
  "FIX": {
    "prefix": "fix",
    "description": "Generate a `FIX` comment.",
    "body": [
      "$LINE_COMMENT FIX: $1 - $CURRENT_MONTH.$CURRENT_DATE.$CURRENT_YEAR: $0"
    ]
  },
  "HACK": {
    "prefix": "hack",
    "description": "Generate a `HACK` comment.",
    "body": [
      "$LINE_COMMENT HACK: $1 - $CURRENT_MONTH.$CURRENT_DATE.$CURRENT_YEAR: $0"
    ]
  }
}
